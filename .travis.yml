sudo: required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - docker-composer exec --env 'RAILS_ENV=test' web rails db:create
  - docker-composer exec --env 'RAILS_ENV=test' web rails db:migrate
  - docker-composer exec --env 'RAILS_ENV=test' web rails test
